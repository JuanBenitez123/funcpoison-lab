{
  "name": "funcpoison-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Orchestrator service (:3000) — loads the tool registry, scores tools, executes the selected tool, forwards output to the analyzer.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/selector.test.ts src/integrity.test.ts"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
