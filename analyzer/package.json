{
  "name": "funcpoison-analyzer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Analyzer service (:3001) — receives orchestrator output; vulnerable version trusts it, patched version validates provenance/integrity before acting.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/downstream.test.ts"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
